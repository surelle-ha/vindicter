{
  "framework": "nuxtjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "nuxt generate",
  "outputDirectory": ".output/public",
  "devCommand": "nuxt dev --port 3005",
  "rewrites": [{ "source": "/:path*", "destination": "/index.html" }]
}
